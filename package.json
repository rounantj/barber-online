{
  "name": "cliente",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build && nodemon dist/app.js",
    "prod": "npm run build && node dist/app.js",
    "build": "tsc && cp -r public dist/",
    "upload": "sshpass -p 'mdt1234@' ssh root@192.241.153.48 'cd /var/family/barber-online && git reset --hard HEAD && git fetch --all && git reset --hard origin/main && git pull && npm run deploy'",
    "deploy": "bash ./deploy.sh"


},
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "@types/handlebars": "^4.1.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-handlebars": "^6.0.7",
    "handlebars": "^4.7.7",
    "hbs": "^4.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.0.1",
    "mongoose": "^6.9.1",
    "nodemon": "^2.0.20",
    "swagger-ui-express": "^4.6.0",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/jsonwebtoken": "^9.0.1",
    "@types/mongodb": "^4.0.7",
    "@types/swagger-ui-express": "^4.1.3"
  }
}
